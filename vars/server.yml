---

apt_keys:
  docker: 
    id: 0EBFCD88
    name: docker.gpg
    url: https://download.docker.com/linux/ubuntu/gpg

apt_repository: 
  docker: deb [arch=amd64] https://download.docker.com/linux/ubuntu trusty stable
  python: ppa:jonathonf/python-3.6
  git: ppa:git-core/ppa

apt_pre_packages:
  - apt-transport-https
  - ca-certificates
  - curl
  - software-properties-common

apt_packages:
  - python-pip
  - python3.6
  - python3-pip
  - vim
  - tmux
  - tree
  - zsh
  - git
  - docker-ce

pip_packages:
  - virtualenv